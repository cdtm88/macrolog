# Phase 04 — friction-pass (provisional)

**Goal:** Remove every avoidable delay and tap between opening the app and a
logged meal.

**Decisions in play:** none new.

**Provisional:** requirements are a starting hypothesis; revise after the
adherence checkpoint.

**Done:** Launch lands on camera < 1.5s; submitting returns control < 200ms with
the estimate resolving in the background; a pending review is visible without
navigation; operable at the largest Dynamic Type size.

**Implementation:** camera default at root, background session configuration,
async submit, ready pill, accessibility labels, scalable fonts.
